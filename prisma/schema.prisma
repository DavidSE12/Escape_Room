

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Question {
  id        Int      @id @default(autoincrement())
  quizKey   String   // e.g. q1, q2, q3
  question  String
  answer    String
  hint      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}


